spring:
  datasource:
    url: "jdbc:tc:postgresql:14:///databasename"
  liquibase:
    change-log: "classpath:db/postgresql/liquibase/changelog.xml"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format-sql: true