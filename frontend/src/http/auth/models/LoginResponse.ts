export class LoginResponse {

    kind: string;

    localId: string;

    email: boolean;

    displayName: string;

    idToken: string;

    registered: boolean;

    refreshToken: string;

    expiresIn: number;
}